#include "funcionescajero.h"
int main(){
	menucajero();
	/*int cantLineas;
    string nombreArchivo = "usuarios.txt";
    string* lineas = iterarArchivo(nombreArchivo, cantLineas);
    for (int i = 0; i < cantLineas; i++) {
        cout << lineas[i] << endl; // Procesar la línea según tus necesidades
    }
    delete[] lineas;
    return 0;
	*/
}